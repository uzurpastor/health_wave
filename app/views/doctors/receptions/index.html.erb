index doctor
<br>

<%= form_tag doctors_index_reception_path, method: :get do %>
  <%= select_tag :status, options_for_select(Reception.statuses.values, @filtering_status) %>
  <%= submit_tag 'filter' %>
<% end %>
<%= render 'receptions/index' %>